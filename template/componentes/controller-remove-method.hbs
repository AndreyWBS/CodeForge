  async remove(req, res, next) {
    try {
      const removed = await this.service.remove(req.params.id);
      if (!removed) return res.status(404).json({ message: 'Registro nao encontrado' });
      return res.status(204).send();
    } catch (error) {
      return next(error);
    }
  }